rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporäre Regel für Entwicklung - erlaubt allen Zugriff
      // ACHTUNG: Nur für Entwicklung verwenden!
      allow read, write: if true;
    }
  }
}
